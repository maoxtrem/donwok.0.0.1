<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}DonWok POS{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
        
        {# SweetAlert2 Local #}
        <script src="/vendor/sweetalert2/sweetalert2.all.min.js"></script>
        
        <style>
            :root {
                --sidebar-width: 260px;
                --primary-blue: #4299e1;
                --dark-blue: #1a365d;
                --light-bg: #f7fafc;
            }

            body {
                background-color: var(--light-bg);
                font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            }

            .sidebar {
                width: var(--sidebar-width);
                height: 100vh;
                position: fixed;
                top: 0;
                left: 0;
                background: var(--dark-blue);
                color: white;
                z-index: 1000;
                display: flex;
                flex-direction: column;
            }

            /* Por defecto ocultamos el sidebar si la página es 'login-page' */
            .login-page .sidebar { display: none !important; }
            .login-page .main-content { margin-left: 0 !important; padding: 0; }

            .sidebar-header {
                padding: 2rem 1.5rem;
                text-align: center;
                border-bottom: 1px solid rgba(255,255,255,0.1);
            }

            .nav-link {
                color: rgba(255,255,255,0.7);
                padding: 0.8rem 1.5rem;
                display: flex;
                align-items: center;
                gap: 12px;
                transition: all 0.2s;
                border-left: 4px solid transparent;
                text-decoration: none;
            }

            .nav-link:hover, .nav-link.active {
                color: white;
                background: rgba(255,255,255,0.05);
                border-left-color: var(--primary-blue);
            }

            .main-content {
                margin-left: var(--sidebar-width);
                padding: 2rem;
                min-height: 100vh;
                transition: margin-left 0.3s ease;
            }

            /* Pantallas completas (Monitor y Login) */
            .login-page .main-content, .monitor-page .main-content { 
                margin-left: 0 !important; 
                padding: 0 !important; 
            }

            .glass-card {
                background: rgba(255, 255, 255, 0.95);
                border-radius: 15px;
                box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            }

            /* Botón Salir Especial */
            .btn-logout-sidebar {
                background: rgba(254, 178, 178, 0.1);
                color: #feb2b2 !important;
                border-radius: 12px;
                border: 1px solid rgba(254, 178, 178, 0.2) !important;
                margin-top: 1rem;
                justify-content: center;
                font-weight: 600;
                transition: all 0.3s ease !important;
            }

            .btn-logout-sidebar:hover {
                background: #f56565 !important;
                color: white !important;
                border-color: #f56565 !important;
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(245, 101, 101, 0.3);
            }

            .btn-logout-sidebar i {
                font-size: 1.1rem;
            }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body class="{% block body_class %}{% endblock %}">
        
        {# Sidebar: Solo si NO es monitor y NO es login #}
        {% if app.request.get('_route') != 'app_pedidos_monitor' and block('body_class') != 'login-page' %}
        <nav class="sidebar">
            <div class="sidebar-header">
                <h4 class="mb-0 fw-bold">🍱 DonWok</h4>
                <small class="opacity-50">Administración</small>
            </div>
            
            <div class="sidebar-menu mt-4">
                <a href="/dashboard" class="nav-link">
                    <i class="bi bi-speedometer2"></i> Dashboard
                </a>
                <a href="/productos" class="nav-link">
                    <i class="bi bi-box-seam"></i> Productos
                </a>
                <a href="/ventas" class="nav-link">
                    <i class="bi bi-cart3"></i> Ventas (POS)
                </a>
                <div class="mt-3 px-3 small text-uppercase opacity-50 fw-bold" style="font-size: 0.7rem; color:white;">Pedidos</div>
                <a href="/pedidos/gestion" class="nav-link">
                    <i class="bi bi-clipboard-check"></i> Gestión de Cola
                </a>
                <a href="/pedidos/monitor" class="nav-link" target="_blank">
                    <i class="bi bi-display"></i> Monitor Público
                </a>
            </div>

            <div class="mt-auto p-4">
                <a href="/login" class="nav-link btn-logout-sidebar" onclick="localStorage.removeItem('authToken')">
                    <i class="bi bi-box-arrow-left"></i> 
                    <span>Cerrar Sesión</span>
                </a>
            </div>
        </nav>
        {% endif %}

        <main class="main-content">
            {% block body %}{% endblock %}
        </main>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>